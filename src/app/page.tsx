import Balance from './balance';

const Home = async () => {
  return (
    <main className="flex min-h-screen flex-col items-center p-5 lg:p-24">
      <Balance />
    </main>
  );
};

export default Home;
